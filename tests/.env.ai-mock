# AI Proxy test env — used with supabase functions serve --env-file
# These are NOT real credentials. The mock key triggers the local mock server.
OPENAI_API_KEY=sk-mock-test-key-not-real
OPENAI_BASE_URL=http://host.docker.internal:9876
