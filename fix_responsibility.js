const { createClient } = require('@supabase/supabase-js'); const supabase = createClient('https://pfqaeewmlwfayxbgmuaq.supabase.co', (process.env.SUPABASE_SERVICE_KEY || (() => { throw new Error('SUPABASE_SERVICE_KEY not set'); })())); async function run() { console.log('Fetching from Frappe...'); const r = await fetch('https://fleetrack.machinery-exchange.com/api/resource/FT%20Breakdown%20Log?limit_page_length=5000&fields=["name","resp"]', { headers: { 'Authorization': 'token 07660480c74686c:82899cba0bfdc36' } }); const json = await r.json(); const data = json.data; console.log('Fetched ' + data.length + ' records from Frappe.'); let count = 0; for(let i=0; i<data.length; i+=100) { const batch = data.slice(i, i+100); const updates = batch.map(b => ({ frappe_name: b.name, responsibility: b.resp })); const { error } = await supabase.from('ft_breakdown_logs').upsert(updates, { onConflict: 'frappe_name', ignoreDuplicates: false }); if(error) console.error(error); else count += batch.length; console.log('Updated ' + count + ' records...'); } console.log('Done!'); } run();
